Don't use `HyperView`s to keep your code DRY. Instead, think about which subsections of a page ought to update independently. Those are `HyperView`s. If you need reusable interactivity, use view functions whenever possible.

See [[/data/sortabletable]] for a more complicated example

