Hyperbole encourages using the [atomic-css](https://github.com/seanhess/atomic-css)  package to factor styles with haskell functions. Here is how we might create reusable header and btn styles:

    #EMBED Example.Docs.CSS import

    #EMBED Example.Docs.CSS example

Note how we use `hover` to provide immediate feedback to the user without talking to the server
