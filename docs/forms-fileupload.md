We can use `fileInput` to accept file uploads. These are saved to a temporary file and cleaned up after the request.

    #EMBED Example.FormFileUpload data DocumentForm

    #EMBED Example.FormFileUpload documentFormView
